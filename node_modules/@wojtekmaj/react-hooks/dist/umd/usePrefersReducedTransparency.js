"use strict";

exports.__esModule = true;
exports["default"] = usePrefersReducedTransparency;

var _useMatchMedia = _interopRequireDefault(require("./useMatchMedia"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/**
 * Returns a flag which determines if the document matches `(prefers-reduced-transparency: reduce)`
 * media feature.
 *
 * @returns {boolean} Whether the document matches `(prefers-reduced-transparency: reduce)` media
 *   feature
 */
function usePrefersReducedTransparency() {
  return (0, _useMatchMedia["default"])('(prefers-reduced-transparency: reduce)');
}