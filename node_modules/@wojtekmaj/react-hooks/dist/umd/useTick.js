"use strict";

exports.__esModule = true;
exports["default"] = useTick;

var _react = require("react");

var _useSetInterval = _interopRequireDefault(require("./useSetInterval"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/**
 * Counts from 0, increasing the number returned every n milliseconds.
 *
 * @param {number} delay Interval between ticks (in ms)
 * @returns {number} Tick
 */
function useTick(delay) {
  if (delay === void 0) {
    delay = 1000;
  }

  var _useState = (0, _react.useState)(0),
      tick = _useState[0],
      setTick = _useState[1];

  var doTick = (0, _react.useCallback)(function () {
    setTick(function (prevSeconds) {
      return prevSeconds + 1;
    });
  }, []);
  (0, _useSetInterval["default"])(doTick, delay);
  return tick;
}