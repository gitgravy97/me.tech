"use strict";

exports.__esModule = true;
exports["default"] = usePrefersReducedMotion;

var _useMatchMedia = _interopRequireDefault(require("./useMatchMedia"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/**
 * Returns a flag which determines if the document matches `(prefers-reduced-motion: reduce)` media
 * feature.
 *
 * @returns {boolean} Whether the document matches `(prefers-reduced-motion: reduce)` media feature
 */
function usePrefersReducedMotion() {
  return (0, _useMatchMedia["default"])('(prefers-reduced-motion: reduce)');
}